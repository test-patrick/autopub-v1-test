name: Autopub

on:
  push:
    branches:
      - main

jobs:
  autopub-release:
    name: Autopub Release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Check
        run: uvx --from autopub==1.0.0a25 --with pygithub autopub check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish
        run: uvx --from autopub==1.0.0a25 --with pygithub autopub publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


